<!DOCTYPE html>
<html class="h-full">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title> {{ title || "Adocasts Simple Layout Example" }} </title>
    @if ($slots.meta)
    {{{ await $slots.meta() }}}
    @endif
    @vite(['resources/js/base.ts'])
  </head>
  <body class="bg-gray-50 dark:bg-slate-900 flex flex-col min-h-full">
    @include('partials/header')
    <main id="content" role="main" class="flex-1">
      {{{ await $slots.main() }}}
    </main>

    @include('partials/footer')
  </body>
</html>
